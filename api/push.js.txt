export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method Not Allowed' });
  }

  const body = req.body;
  console.log('✅ Новый донат:', body);

  res.status(200).json({ message: 'OK' });
}
